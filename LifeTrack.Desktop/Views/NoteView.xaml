<UserControl x:Class="LifeTrack.Desktop.Views.NoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LifeTrack.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- Not Listesi -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Notlarım" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

            <ListView Grid.Row="1" ItemsSource="{Binding Notes}" 
                      SelectedItem="{Binding SelectedNote}" 
                      BorderThickness="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,5">
                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:dd MMM yyyy, HH:mm}'}" 
                                       FontSize="11" Opacity="0.7" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Not Detayı / Editör -->
        <Grid Grid.Column="1" Margin="20,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Not Detayı" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

            <TextBox Grid.Row="1" Text="{Binding NewNote.Title, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,10" Padding="5" />

            <TextBox Grid.Row="2" Text="{Binding NewNote.Content, UpdateSourceTrigger=PropertyChanged}" 
                     TextWrapping="Wrap" AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto" Padding="5" />

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Yeni Not" Command="{Binding AddNoteCommand}" 
                        Padding="10,5" Margin="0,0,5,0" />
                <Button Content="Güncelle" Command="{Binding UpdateNoteCommand}" 
                        Padding="10,5" Margin="0,0,5,0" />
                <Button Content="Sil" Command="{Binding DeleteNoteCommand}" 
                        Padding="10,5" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>